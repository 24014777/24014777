#include <stdio.h>

int main() {
   
    int a;         // Declaração da variável
    a = 10;        // Inicialização da variável
    printf("%d\n", a); // Correção na formatação do printf
    
  return 0;
}
