#include <stdio.h>

int main() {
    int RA, ultimo;
    double resultado

    printf("Digite o seu RA: ");
    scanf("%d", &RA);

   

    printf("O último digito do seu RA: %d é: %d\n", RA, ultimo);
resultado= ultimo/2
    if (resultadoo-0.5==(int)resultado){
        printf("O seu ultimo digito é PAR.\n");
        } else {
        printf("O seu ultimo digito é ÍMPAR.\n");
}
    return 0;
}
